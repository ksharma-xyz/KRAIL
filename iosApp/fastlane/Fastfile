# Fastfile for KRAIL iOS App

default_platform(:ios)

platform :ios do
  desc "Build iOS App for Release"
  lane :build_release do
    setup_ci if ENV['CI']

    # Xcode version is selected by maxim-lobanov/setup-xcode in the CI workflow.
    # xcversion is removed — the underlying xcode-install gem is sunsetted and
    # xcversion is broken on macos-15 runners (fastlane/fastlane#29469).

    # Build the app.
    # xcargs forces manual signing during the archive phase — without this, Xcode tries
    # to manage signing automatically and fails in CI where it can't reach Apple servers.
    # export_options.provisioningProfiles covers the export phase.
    build_app(
      scheme: "iosApp",
      project: "iosApp.xcodeproj",
      configuration: "Release",
      export_method: "app-store",
      output_directory: "./build",
      output_name: "iosApp.ipa",
      clean: true,
      # CODE_SIGN_STYLE=Manual overrides automatic signing for the archive phase.
      # PROVISIONING_PROFILE_SPECIFIER is intentionally NOT passed via xcargs — it
      # applies to every target including SPM resource bundles and breaks MapLibre.
      # Instead, Xcode locates the downloaded profile by bundle ID + identity, and
      # export_options.provisioningProfiles maps it explicitly for the export phase.
      xcargs: "CODE_SIGN_STYLE=Manual CODE_SIGN_IDENTITY='Apple Distribution' DEVELOPMENT_TEAM='#{ENV["DEVELOPMENT_TEAM"]}'",
      export_options: {
        signingStyle: "manual",
        provisioningProfiles: {
          ENV["APP_BUNDLE_ID"] => ENV["PROVISIONING_PROFILE_NAME"]
        }
      }
    )
  end

  desc "Upload to TestFlight"
  lane :upload_testflight do
    # Upload to TestFlight
    upload_to_testflight(
      api_key_path: ENV["APP_STORE_CONNECT_API_KEY_PATH"],
      skip_waiting_for_build_processing: true,
      skip_submission: true,
      distribute_external: false,
      notify_external_testers: false
    )
  end

  desc "Build and Upload to TestFlight"
  lane :release do
    build_release
    upload_testflight
  end
end

