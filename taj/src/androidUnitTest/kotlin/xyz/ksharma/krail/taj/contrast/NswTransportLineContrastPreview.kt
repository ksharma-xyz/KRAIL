// This file is intentionally empty.
// The NSW Transport Line contrast catalog previews have been moved to:
//   taj/src/commonMain/…/taj/contrast/NswTransportLineContrastPreview.kt
//
// Previews annotated with @ScreenshotTest must live in commonMain so that the
// ComposablePreviewScanner used by TajSnapshotTest can locate them via the
// shared classpath, and so that they benefit from commonMain compose dependencies
// rather than needing duplicated androidUnitTest dependency declarations.
package xyz.ksharma.krail.taj.contrast

