package xyz.ksharma.krail.core.transport

/** Sort order for transport mode lists — city-agnostic, used by every TransportConfig. */
enum class TransportModeSortOrder { NAME, PRIORITY, PRODUCT_CLASS }
